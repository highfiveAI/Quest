# Python 3.12 슬림 이미지를 기반으로 경량 컨테이너 생성
FROM python:3.12-slim

# 컨테이너 내부 작업 디렉토리를 /app으로 설정
WORKDIR /app

# 의존성 파일만 먼저 복사 → 캐시 재활용을 위해
COPY requirements.txt .

# 의존성 설치
RUN pip install --no-cache-dir -r requirements.txt

# 나머지 애플리케이션 파일 전체 복사
COPY . .

# FastAPI 서버용 기본 포트
EXPOSE 8000

# uvicorn으로 서버 실행 (main.py의 app 객체 기준)
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]